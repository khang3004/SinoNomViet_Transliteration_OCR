# OCR-review runtime deps.
#
# This stage reads no images and runs no models: it fetches the upstream team's
# pictures from Google Drive, shows them to reviewers, and records verdicts.

# Web layer
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Auth
bcrypt==4.2.0
PyJWT==2.9.0

# RS256, for signing the Google service-account assertion. PyJWT cannot do
# RS256 without it — HS256 (used for our own session cookies) needs nothing.
cryptography==43.0.1

# Google Drive listing + image mirroring
httpx==0.27.2

# Prefill: drafts a review from the picture (app/core/assist.py)
google-genai==2.22.0

# Their ground_truth.xlsx in, our reviews.xlsx out
openpyxl==3.1.5

# Image dimensions on the review screen
Pillow==10.4.0

# Host telemetry for the monitoring panel
psutil==6.0.0
