# Image-prep runtime deps.
#
# No OCR stack: this stage downloads images and serves them, it does not read
# them. Dropping paddleocr/paddlepaddle/numpy took the image from ~3 GB to a few
# hundred MB and the build from ~15 minutes to about one.
#
# Pillow stays — image_dimensions() in app/core/imagestore.py records width and
# height on every output record.

# Web layer
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Auth
bcrypt==4.2.0
PyJWT==2.9.0

# Storage + HTTP
minio==7.2.9
httpx==0.27.2
Pillow==10.4.0

# Host telemetry for the monitoring UI
psutil==6.0.0
