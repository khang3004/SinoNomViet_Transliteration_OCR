# Dev/test deps. The test suite deliberately needs none of the heavy runtime
# stack — no paddleocr, no minio, no fastapi — so it runs fast anywhere:
#
#   python -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
#   .venv/bin/python -m pytest
pytest==9.1.1
pytest-asyncio==1.3.0
pytest-cov==7.0.0
