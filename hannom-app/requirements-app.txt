# app service — lightweight, NO GPU (AGENTS.md §10).
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Postgres (job queue + records) and self-hosted login (JWT cookie + bcrypt).
psycopg[binary]==3.2.3
PyJWT==2.9.0
bcrypt==4.2.0

# LLM provider SDKs — the app makes translation/correction calls with the
# USER's own key (per request). Each is imported lazily, only when used.
# (DeepSeek is OpenAI-compatible and uses the openai SDK with a custom base_url.)
google-genai>=1.20.0          # current Gemini SDK (replaces google-generativeai)
openai==1.54.4
anthropic==0.39.0
# Pin httpx < 0.28: openai/anthropic 1.54/0.39 pass a `proxies` kwarg that httpx
# 0.28 removed (causes "unexpected keyword argument 'proxies'" on client init).
httpx==0.27.2
